name: testing
on:
  workflow_dispatch:
jobs:
  production-deploy:
    if: github.repository == 'hmcts/actions'
    runs-on: ubuntu-latest
    steps:
      - name: bashing
        run: echo "HEllo"
    
