name: testing
on:
  workflow_dispatch:
jobs:
  production-deploy:
    if: github.repository == 'hmcts/actions'
  run-on: ubuntu-latest
  steps:
    - name: bashing
      uses: rethab/bash-action@v1
      with:
        echo: "HEllo"
    
